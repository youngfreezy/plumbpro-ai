/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: "standalone", // Enable for Docker builds without (route-group) paths
};

export default nextConfig;
